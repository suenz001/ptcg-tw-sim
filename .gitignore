node_modules/
.svelte-kit/
build/
dist/
*.log
.DS_Store
Thumbs.db

.env
.env.*
!.env.example

# v4.481：Firebase → Oracle 搬遷計畫書（含明文密碼）— 純本機文件，不 push
FIREBASE_TO_ORACLE_MIGRATION_PLAN.md

.vscode/
.idea/
*.swp

.claude/

# Scrape outputs — logs and in-progress files (final JSONs ARE committed)
static/cards/*.log
static/cards/*.tmp

# Sim / build temp files
.tmp-sim-*
.tmp-H-*
.tmp-festival-test-*
.sim-test.mjs
.tmp-sim-bundle.mjs
.tmp-all-presets-*
.tmp-p2-test-*
.tmp-test-evolve-iid-*
.tmp-audit-preset-card-coverage-*


# 錦標賽伺服器引擎 bundle/pool（產生檔，部署時 build-server-engine.mjs 重建）
oracle-admin/tournament/server-engine.cjs
oracle-admin/tournament/tournament-pool.json
oracle-admin/tournament/.engine-entry.ts
oracle-admin/tournament/.appstub.js

# ⭐⭐⭐v6.384：Firebase Admin 服務帳戶金鑰（裡面是明文私鑰）——絕不可以進版控。
#   ⚠ 這個檔案一直躺在工作樹裡，而 .gitignore 從來沒有擋它。v6.384 有一次 `git add -A`
#     把它 staged 進 commit（當場 `git reset` 撤銷；查證過它從未進過任何一個 commit）。
#   ⇒ 補上這兩條，讓同樣的手滑不可能再發生一次。
oracle-admin/firebase-admin-key.json
oracle-admin/*firebase*key*.json

# AI 交辦用的臨時腳本與輸出（純本機，不進版控）
__m6a/

# ════════════════════════════════════════════════════════════════════════════
# 守衛的 esbuild 暫存產物（repo 根與 scripts/ 下的點開頭檔）
# ────────────────────────────────────────────────────────────────────────────
# 為什麼要列管：站內 582 支守衛會往 repo 根寫 `.x-s.js` / `.x-e.ts` / `.x-o.mjs`
# 這類暫存檔，收尾靠 `process.on('exit', () => unlinkSync(...))`。Windows 不允許
# 刪除仍被開啟的檔（剛 import() 過的 ESM 模組），那個 unlink 會丟 EPERM 被
# catch 吞掉；中斷（Ctrl-C／逾時被殺）時更是完全不清。
# 實測後果：2026-09 曾累積到 405 個（301MB），其中
#   scripts/.v6380-positive-control.mjs（內容 import 一個不存在的套件）
# 被 test-v6380 自己的 listFiles() 掃到（它只判副檔名、不濾點開頭）⇒
# B1/B2/D2 三條翻紅。那是一個純粹由殘檔造成的假紅。
#
# ⚠ gitignore **不支援大括號展開**：`.*.{ts,js,mjs}` 是無效語法（會被當字面量），
#   必須逐行列。
#   實測殘檔副檔名分布：.mjs 184 / .ts 136 / .js 98 / .txt 2（無 .json）
# ⚠ 刻意**不**列 `/.*.json`：追蹤集裡有 `.tmp-H-classify.json`，列了會讓
#   `git check-ignore` 對一個追蹤中的檔回報 ignored，徒增混淆。
# ════════════════════════════════════════════════════════════════════════════
/.*.ts
/.*.js
/.*.mjs
/.*.cjs
/scripts/.*.ts
/scripts/.*.js
/scripts/.*.mjs
/scripts/.*.cjs
/scripts/.*.txt
/scripts/tmp*.mjs

# 平行 runner 的沙盒報表（放在沙盒根旁邊，正常不會進 repo，保險起見列管）
/__rt/

# 建置中繼與工作產物（都不進版控；實測未追蹤檔裡這三類佔 316/609）
.svelte-kit-staging/
tournament-dumps/
__*/
