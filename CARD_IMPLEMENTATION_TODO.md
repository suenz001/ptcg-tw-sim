# PTCG 常用 ex / 超級進化卡牌實裝代辦

最後更新：2026-05-03（v2.340 起）

用途：保存常用寶可夢 ex / 超級進化寶可夢的審計結果與實裝批次，避免更換 model 或 context 壓縮後遺漏。

## 審計原則

- 卡文必須從 `static/cards/*.json` 驗證，不可憑記憶。
- 審計不能只看 `ABILITY_EFFECTS` / `ATTACK_PRE` / `ATTACK_POST`。
- 必須同時檢查：
  - `regA(...)` 主動特性
  - `regPre(...)` / `regPost(...)` 招式效果
  - `regR(...)` / `RESOLVERS` pending selection 第二階段
  - `PASSIVE_*` 被動效果 maps
  - `ABILITY_RETREAT_MOD`
  - `engine.ts` hard-code / gate / UI ability availability
  - `effects/cards/*` side-effect import batch
- 純傷害招式通常不需要額外 hook；只要 engine 能正常套用一般傷害即可。
- G 標卡原則不實裝；但使用者明確指定時可實作。

## 本批使用者清單與狀態

| 卡牌 | 狀態 | 備註 |
|---|---:|---|
| 幸福蛋ex | 部分 | `報恩` 已實裝；`幸福切換` 未實裝 |
| 厄鬼椪 火灶面具ex | 已實裝 | `憤怒之窯`、`極限火焰` 已實裝 |
| 甲賀忍蛙ex SV5a 090/066 | 已實裝 | 使用者寫 V5a；JSON 實際為 SV5a。`忍之利刃`、`分身連打` 已實裝 |
| 來悲粗茶ex | 部分 | `抹茶飛濺` 已實裝；`熬返` 未實裝 |
| 鐵荊棘ex | 部分 | `伏特旋風` 已實裝；`初始化` 未完整實裝 |
| 熾焰咆哮虎ex | 部分 | `火焰炸彈` 已實裝；`喧鬧競技` 未實裝 |
| 波盪水ex | 已實裝 | `宣洩吼嘯`、`藏青浪濤` 已實裝 |
| 土台龜ex | 已實裝 | `森林行進`、`叢林之錘` 已實裝 |
| 耿鬼ex SV5K 047/071 | 未實裝 | 需補本體；目前找到的是 `超級耿鬼ex` 不是這張 |
| 倫琴貓ex | 部分 | `伏特強襲` 已實裝；`突刺目光` 未實裝 |
| 厄鬼椪 礎石面具ex | 已實裝 | `礎石之勢`、`打爆` 已實裝 |
| 願增猿ex | 部分 | `惡劣頭擊` 已實裝；`鬆口氣` 未實裝 |
| 夠讚狗ex | 部分 | `瘋狂連鎖` 已實裝；`猛毒筋力` 未實裝 |
| 蜜集大蛇ex | 已實裝 | `熟成充能`、`蜜糖風暴` 已實裝 |
| 閃焰王牌ex | 已實裝 | `閃焰強襲`、`石榴石截擊` 已實裝 |
| 麻花犬ex | 部分 | `奇跡閃耀` 已實裝；`飽腹時間` 未實裝 |
| 恰雷姆ex | 已實裝 | `氣功指壓`、`瑜伽踢` 已實裝 |
| 太樂巴戈斯ex | 部分 | `聯盟擊` 已實裝；`皇冠蛋白石` 未實裝 |
| 拉普拉斯ex | 部分 | `力量飛濺` 已實裝；太晶版本其他效果未完整 |
| 阿羅拉 椰蛋樹ex | 部分 | `熱帶狂燒` 已實裝；`嗡嗡榍石` 未實裝 |
| 鐵蟻ex | 已實裝 | `突然削退`、`復仇粉碎` 已實裝 |
| 美納斯ex | 部分 | `昏睡飛濺` 已實裝；`璀璨鱗片` 目前只擋太晶傷害，效果免疫未完整 |
| 超級沙奈朵ex | 未實裝 | `盈溢祈願`、`超級交響樂` 未找到實裝 |
| 超級暴雪王ex | 未實裝 | `山崩之錘`、`冰霜屏障` 未找到實裝 |
| 萊希拉姆ex | 未實裝/部分 | 純傷害不需 hook；`火爆發` 未實裝 |
| 遠古巨蜓ex | 已實裝 | 掃描到相關招式效果已實裝 |
| 夢妖魔ex | 未實裝 | `漩渦言靈`、`六之魔法` 未找到實裝 |
| 超級巨牙鯊ex | 部分 | `貪心之牙` 已實裝；`飢渴下巴` 未實裝 |
| 阿響的鳳王ex | 未實裝 | `金色火焰`、`閃耀羽毛` 未找到實裝 |
| 捷克羅姆ex | 未實裝/部分 | 使用者重複列出；純傷害不需 hook；`電爆發` 未實裝 |
| 奇樹的電肚蛙ex | 部分 | `電氣流` 未實裝；攻擊副作用需再驗證 |
| 超級摔角鷹人ex | 未實裝 | `堅忍之軀`、`筋斗強襲` 未找到實裝 |
| 超級頭巾混混ex | 部分 | `反擊雞冠` 已實裝；`不法之足` 未實裝 |
| 三首惡龍ex | 部分/已實裝 | SV8 版本 `黑曜石`、`粉碎頭` 已實裝；若為新版本含 `貪婪食客/暗黑啃咬` 則未實裝 |
| 赫普的蒼響ex | 未實裝 | `剎那斬`、`無畏斬` 未找到實裝 |
| 波爾凱尼恩ex | 未實裝 | `燒灼蒸汽`、`高溫旋風` 未找到實裝 |
| 超級盔甲鳥ex | 已實裝 | `音波拆裂` 已實裝 |
| 焰后蜥ex | 未實裝 | `詭計`、`剋命銳爪` 未找到實裝 |
| 大針蜂ex | 未實裝 | `針蜂轟鳴` 等效果未找到實裝 |
| 超級花葉蒂ex | 未實裝 | 目前只找到 stadium `昂主花葉蒂` 對它的 HP 加成；本體未實裝 |
| 伊布ex | 已實裝/不需 hook | `虹色DNA` 已在 engine hard-code；`石英閃耀` 純傷害不需 hook |

## 實裝批次規劃

### v2.341 A 批：優先常用/影響規則

目標：先補常用且對對局狀態影響大的缺口。

- [ ] 鐵荊棘ex｜初始化
  - 戰鬥場時，雙方場上「擁有規則的寶可夢」（未來寶可夢除外）特性消除。
  - 需 engine-level ability block / getUsableAbilities / passive gate。
- [ ] 耿鬼ex SV5K 047/071
  - 需從 JSON 驗證精確卡文與 collector number。
  - 補招式/特性，並加 focused tests。
- [ ] 幸福蛋ex｜幸福切換
  - 自己回合 1 次，選自己場上 1 個基本能量改附到其他自己的寶可夢。
  - 使用 pending selection / resolver 實作。
- [ ] 來悲粗茶ex｜熬返
  - 展示棄牌區所有基本草能量，依張數 ×2 個傷害指示物放在對手 1 隻寶可夢，然後那些能量洗回牌庫。
  - 需 target selection + discard-to-deck shuffle。
- [ ] 倫琴貓ex｜突刺目光
  - 需從 JSON 驗證卡文與觸發時點。
  - 若為進場/進化觸發，需接 evolution trigger 或特性使用流程。

驗證：
- 新增 `scripts/test-v2341-common-ex-batch.mjs`
- 跑新 focused test
- 跑既有：`node scripts/test-cinccino-deck-focused.mjs`、`node scripts/test-p2-abilities.mjs`、`node scripts/test-m2-dragon-charizard-batch.mjs`
- `npm run build`
- 更新 `src/lib/version.ts`、`AI_HANDOFF.md`
- commit + push

### B 批：特性/狀態/條件效果補齊

- [ ] 熾焰咆哮虎ex｜喧鬧競技
- [ ] 願增猿ex｜鬆口氣
- [ ] 夠讚狗ex｜猛毒筋力
- [ ] 麻花犬ex｜飽腹時間
- [ ] 奇樹的電肚蛙ex｜電氣流
- [ ] 超級頭巾混混ex｜不法之足
- [ ] 超級巨牙鯊ex｜飢渴下巴

### C 批：太晶 / 防護 / 全場效果

- [ ] 太樂巴戈斯ex｜皇冠蛋白石
- [ ] 拉普拉斯ex｜海紋石之雨
- [ ] 阿羅拉 椰蛋樹ex｜嗡嗡榍石
- [ ] 美納斯ex｜璀璨鱗片完整化
- [ ] 超級暴雪王ex｜冰霜屏障
- [ ] 超級摔角鷹人ex｜堅忍之軀

### D 批：剩餘整張未實裝 ex / Mega ex

- [ ] 超級沙奈朵ex
- [ ] 夢妖魔ex
- [ ] 阿響的鳳王ex
- [ ] 波爾凱尼恩ex
- [ ] 焰后蜥ex
- [ ] 大針蜂ex
- [ ] 超級花葉蒂ex
- [ ] 萊希拉姆ex
- [ ] 捷克羅姆ex
- [ ] 赫普的蒼響ex

## 進行中狀態

- 下一步：開始 v2.341 A 批。
