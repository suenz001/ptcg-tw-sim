/**
 * 應用程式版本號。
 *
 * 規則：
 * - 小更新（bug fix / 小 UI 調整）：+0.01（例 1.0 → 1.01 → 1.02）
 * - 大更新（新功能 / 新機制）：+0.1（例 1.09 → 1.1）
 * - 重大變革：+1（例 1.x → 2.0）
 *
 * 每次 push 新版本前更新此字串，UI 會顯示在頁首，
 * 讓使用者確認瀏覽器是否同步到最新版本（vs 快取舊版）。
 */
export const VERSION = '2.67';
