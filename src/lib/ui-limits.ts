/**
 * ⭐v6.389 版面上限的**單一來源**（UI 與守衛共用，禁止各寫一份）。
 *
 * 起因（玩家回報）：夢幻ex｜記憶螺旋「這隻寶可夢可使用自己的備戰寶可夢持有的所有招式。」
 * 讓招式清單長到十幾條，**後面的招式按不下去**。
 *
 * ⚠ 根因不是「清單長」，是 Fable 版（桌機 ≥1024px 的預設版面）只釘死了 **3 個招式槽**：
 *   `+page.svelte` 的 `.atk-slot:nth-of-type(1|2|3){ grid-row:1|2|3 }`，
 *   第 4 個以後沒有任何 grid-row 規則 ⇒ CSS Grid 把它們排到「悔棋」按鈕**下方**，
 *   再被 `.playmat.layout-fable{ overflow:hidden }` ＋ `.battle-root{ overflow:hidden }` **實體裁掉**
 *   —— 不是被蓋住，是根本沒被畫出來，而且整頁還捲不動。
 *
 * ⚠⚠ 所以這**不是記憶螺旋專屬的坑，是「超過 3 招就會發生」**：
 *   ・古空棘魚｜潛入記憶（v3.08 起）配 2 階進化 ⇒ 7 招
 *   ・洛托姆ex｜多重轉接 帶 2 張技術機 ⇒ 4 招
 *   記憶螺旋只是把這個舊坑踩穿（備戰 8 隻 × 每張最多 3 招 ⇒ 理論上界 26 招）。
 *
 * 站長 2026-09-15 裁示：閾值 **3**、≤3 招**維持現狀**（零位移零回歸）。
 */

/**
 * 招式清單「直接排在行動列上」的張數上限。
 * 超過這個數 ⇒ 收成一顆「⚔ 選擇招式（N）」，點開 picker（position:fixed 的 modal，
 * 不可能被卡片蓋住、不可能被 overflow:hidden 裁掉）。
 *
 * ⚠ 這個值**對齊 Fable 版釘死的 3 個 grid-row 槽**。要改的話，
 *   `.atk-slot:nth-of-type(n)` 的規則也要同步加，否則第 4 槽又會掉到悔棋下面。
 */
export const ATTACK_LIST_INLINE_MAX = 3;
