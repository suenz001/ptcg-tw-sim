/**
 * v6.191：Gust 系支援者（選 1 隻對手備戰寶可夢與戰鬥寶可夢互換）的**卡名單一來源**。
 *
 * 站內 reg key 是**卡名**逐字比對 ⇒ 卡面相同但卡名多了冠名的新印刷**不會自動生效**。
 * 兩個消費點（effects 註冊、AI 手牌判定）一律讀這份清單，禁各自抄一份卡名。
 *
 * v6.193：M-P 215/M-P（I 標，id 19630）站長裁定改名為「老大的指令」⇒ 清單裡的
 *   「老大的指令（烏羽）」已刪除（留著＝零產出的死條目），該印刷直接吃既有
 *   「老大的指令」那份註冊；行為端守衛見 scripts/test-v6193-hk-dup-and-boss-rename.mjs。
 *
 * ⚠ 這個檔**不得 import 任何東西**：ai.ts 與卡檔都要用它，任何相依都可能造成
 *   循環 import（循環下模組層級 const 會 TDZ）。
 */
export const GUST_SUPPORTER_NAMES: readonly string[] = [
  '老大的指令',
];
